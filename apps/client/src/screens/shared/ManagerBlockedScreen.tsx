import { RoleBlockedScreen } from '@/screens/shared/RoleBlockedScreen';

export function ManagerBlockedScreen() {
  return (
    <RoleBlockedScreen
      title="Restaurant tools are on the web"
      body="Menus and incoming orders are managed on the QuickBite manager site. This app is for ordering and, if your account is a rider, for deliveries."
    />
  );
}
